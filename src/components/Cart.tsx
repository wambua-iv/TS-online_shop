
import { item } from "../App"
import { CartWrapper } from "../styles/Cart.style"

type Props ={
    items : any
}
 
const Cart: React.FC<Props> = ({items}) => {
    return (
        <CartWrapper>
            
        </CartWrapper>
    )
}

export default Cart
